# CLAUDE.md — ConnectingPC

@AGENTS.md

## Note specifiche per Claude
- Il vault si legge in locale (`C:\Progetti\DEV\MCP - dev.alexopizzi.it\progetti\ConnectingPC\knowledgebase\`); se il percorso locale non è disponibile (es. sessione cloud), usa il connettore MCP «Bridge - Alex Opizzi» sul percorso server indicato in `AGENTS.md`.
- Aggiorna le note del vault quando emergono decisioni, conclusioni o promemoria, senza chiedere conferma per gli aggiornamenti minori; mantieni l'indice `00 - Indice.md` aggiornato con un wikilink per ogni nota nuova.
- Numerazione delle note: `0x` orientamento · `1x` governo · `2x` analisi · `3x` architettura · `4x` dati · `5x` accessi e workflow · `6x` funzionalità · `7x` trasversali · `8x` operazioni · `9x` archivio.
- Claude Design si usa solo per design system e prototipi dell'area pubblica (vault `73 - Brief per Claude Design.md`).
